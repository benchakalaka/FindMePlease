<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <LinearLayout
        android:id="@+id/layout"
        android:layout_width="fill_parent"
        android:layout_height="58dp"
        android:background="@drawable/custom_border"
        android:orientation="horizontal" >

        <!-- IMAGEVIW/EDITTEXT BLOCK -->

        <ImageView
            android:id="@+id/dialog_inputnumber_imageview_lockimage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleType="fitCenter"
            android:src="@drawable/unlockapp" />

        <EditText
            android:id="@+id/dialog_inputnumber_edittext_phonenumber"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:inputType="phone"
            android:hint="@string/hint_phone_number_for_unlocking"
            android:textColor="#388E8E"
            android:textStyle="bold" >

            <requestFocus />
        </EditText>
    </LinearLayout>

    <!-- BOTTOM OK/CANCEL BUTTONS BLOCK -->

    <Button
        android:id="@+id/dialog_unlocknumber_button_cancel"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/layout"
        android:layout_marginTop="20dp"
        android:text="@string/cancel" />

    <Button
        android:id="@+id/dialog_unlocknumber_button_ok"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@id/layout"
        android:layout_marginTop="20dp"
        android:text="@string/ok" />

</RelativeLayout>